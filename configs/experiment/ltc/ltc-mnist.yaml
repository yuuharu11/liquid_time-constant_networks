default:
  - /pipeline: default
  - /model: ltc

#fundamental settings
model:
  dropout: 0.0
  n_layers: 4
  d_model: 256
  prenorm: true
  norm: batch

dataset:
  l_max: 2048
  level: char

loader:
  batch_size: 50

optimizer:
  lr: 0.05


trainer:
  max_epochs: 10

train:
  seed: 1112
  test: True