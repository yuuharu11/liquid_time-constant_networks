# @package _group_
defaults:
  - /pipeline: default
  - /model: lstm  
  - override /dataset: uci-har

dataset:
  l_max: 4096
  level: char

loader:
  batch_size: 16

optimizer:
  lr: 0.05
  weight_decay: 0.05

scheduler:
  num_training_steps: 50000

trainer:
  max_epochs: 32

train:
  seed: 2222