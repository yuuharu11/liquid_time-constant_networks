# @package _global_
defaults:
  - /pipeline: default
  - /model: cnn_uci_har
  - override /dataset: uci_har

model:
  _name_: cnn_uci_har
  d_model: 9
  d_output: 6

loader:
  batch_size: 50     

optimizer:
  lr: 1e-4

scheduler:
  num_warmup_steps: 100
  num_training_steps: 4000

decoder:
  _name_: id

encoder: 
  _name_: id