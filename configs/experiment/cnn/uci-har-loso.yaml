# configs/experiment/cnn/uci_har_loso.yaml
# @package _global_

defaults:
  - /pipeline: default
  - /model: cnn_har
  - /dataset: uci_har

trainer:
  max_epochs: 50

optimizer:
  lr: 1e-4
  weight_decay: 0.001

scheduler:
  num_warmup_steps: 100
  num_training_steps: 4000 

model:
  _name_: cnn_har
  d_model: 9
  d_output: 6

loader:
  batch_size: 128     

decoder:
  _name_: id

encoder: 
  _name_: id